# Optional — Google Maps Embed API key (Cloud Console → enable “Maps Embed API”).
# Without a key the contact page still shows the office pin via the public embed URL.
NUXT_PUBLIC_GOOGLE_MAPS_API_KEY=
