// Marketing route group for P02+ pages. Empty pass-through for now — P01
// only needs the group to exist so later phases have a place to land.
export default function SiteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
